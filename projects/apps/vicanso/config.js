(function() {
  var APP_NAME, config;

  APP_NAME = 'vicanso';

  config = {
    /**
     * [getAppName 返回app的名字]
     * @return {[type]} [description]
    */

    getAppName: function() {
      return APP_NAME;
    }
  };

  module.exports = config;

}).call(this);
